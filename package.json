{
	"name": "markdown-it-mathjax3",
	"type": "module",
	"version": "4.4.3",
	"license": "AGPL-3.0-only",
	"main": "dist/main.js",
	"scripts": {
		"rst": "pnpm up -f && pnpm postupd",
		"upd": "pnpm up --latest",
		"make": "pnpm up -s && pnpm test && pnpm build",
		"test": "tsc --noemit",
		"build": "tsc -d",
		"postupd": "replace-in-file \"<!doctype\" \"<!DOCTYPE\" node_modules/prettier/*/*",
		"postbuild": "prettier --no-semi -w dist/"
	},
	"dependencies": {
		"@mathjax/src": "^4.1.3",
		"juice": "^12.1.1"
	},
	"devDependencies": {
		"@types/markdown-it": "^14.1.2",
		"@types/node": "^26.1.1",
		"markdown-it": "^14.3.0",
		"prettier": "^3.9.5",
		"replace-in-file": "^8.4.0",
		"typescript": "^7.0.2"
	},
	"engines": {
		"node": ">=24.0.0",
		"pnpm": ">=10.0.0"
	},
	"pnpm": {
		"overrides": {
			"encoding-sniffer": "^1"
		},
		"packageExtensions": {
			"@mathjax/mathjax-newcm-font": {
				"peerDependencies": {
					"@mathjax/src": "*"
				}
			}
		}
	}
}
